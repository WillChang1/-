# 1. Приветствие
# 2. Выдача мануала игру
# 3. Создание русско-английского словаря
# 4. Описание алгоритма сбора слов
# 4.1. Сбор 4 образца (произвольным образом) -> Машина, Ракета, Школа, Привет
# 4.2. Сбор 1 слова Который являтся вопросом -> ракет
# 4.3. Задать вопрос игроку -> Переводите "Ракета"
# 4.4. Вывод на экран вариантов ответа
# 5. Получение ответа от игрока
# 6. Сравнение ответов и выдача ответа машины
import random

print("Привет, игрок!") # 1

print("Вам будет дано слово на русском языке, также будет выдано 4 варианта ответа на английском. Выберите самый подходящий правильный ответ на ваш взгляд!") # 2

WORDS = {
  'Привет': 'Hello',
  'Пока': 'Goodbye',
  'Запустить': 'Run',
  'Ракета': 'Rocket',
  'Школа': 'School',
  'Змея': 'Python',
  'Машина': 'Car',
  'Язык': 'Language'
} # 3

random_words = random.sample(WORDS.keys(), 4) # 4.1.

random_word = random.choice(random_words) # 4.2.

print(f"Переведите слово '{random_word}' на аглийский язык") # 4.3.

for index, word in enumerate(random_words): # word = random_words[0], index = 0 ...
  print(f"{index + 1}. {WORDS[word]}") # 4.4.

player_answer = int(input("введите ваш ответ: ")) # 5

if random_words[player_answer - 1] == random_word:
  print("Верно!")

else:
  print(f"Введите число соответсвтующее одному из вариантов: {WORDS[random_word]}") # 6



# Защита от выхода за пределами списка random_words
# Инверсия словаря по выбору игрока
# Выбор уровень сложности
